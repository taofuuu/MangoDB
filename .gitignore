node_modules
.env
.DS_Store
Claude.md
AGENTS.md

# graphify — derived knowledge graph + local agent integration
graphify-out/
# repeated at apps/api, but for graphify to read (only root)
apps/api/src/generated/
.claude/
/CLAUDE.md

# Claude Code — machine-local only; apps/*/CLAUDE.md stay tracked
CLAUDE.local.md
